// Solution Example
const animals = ["Reindeer", "Wolf", "Cat"];

const printListNames = animalList => {
  for(let i = 0; i < animalList.length; i++) {
    console.log(animalList[i]);
  }
};
printListNames(animals);